algoritmo "Contador de 10 a 0"
var
   Contador : Real
inicio

Contador <- 10
Enquanto (Contador >= 0 ) faca
      Contador <- Contador - 3
      Escreval(Contador)
FimEnquanto
Escreval("Acabei!")

fimalgoritmo
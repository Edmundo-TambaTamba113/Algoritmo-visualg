algoritmo "Contador de 10 a 0"
var
   Contador : Real
inicio

Contador <- 0
Enquanto (Contador <=10) faca
      Contador <- Contador + 1
      Escreval(Contador)
FimEnquanto
Escreval("Acabei!")

fimalgoritmo